{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Console (dev)",
      "request": "launch",
      "command": "npm run start:dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
