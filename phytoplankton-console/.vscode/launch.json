{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Console (Local API)",
      "request": "launch",
      "command": "nvm use 20 && yarn start:local",
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "node-terminal",
      "name": "Run Console (Dev API)",
      "request": "launch",
      "command": "npm run start:dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
