// todo: just a stub, need to use actual type from API
export type WorkflowItem = {
  id: string;
  category: string;
  name: string;
  description: string;
  type: string;
  createdAt: string;
  status: string;
};
